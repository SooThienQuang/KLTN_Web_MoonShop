@if (@ViewBag.user != null)
{
    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown" id="profileDropdown" style="padding:0">
        <img src="~/Asset/img/user/@ViewBag.user.customerPhoto" />
        <span class='badge badge-warning' id='lblCartCount'>1</span><span class="text-white">Chào @ViewBag.name !</span>
    </a>
    <div class="dropdown-menu dropdown-menu-right navbar-dropdown" aria-labelledby="profileDropdown">
        <p class="mb-0 font-weight-normal float-left dropdown-header">@ViewBag.user.customerName</p>
        <a class="dropdown-item" href="@Url.Action("DetailProfile","Customer",new {id=2})">
            @*<i class="ti-wallet text-primary"></i>*@
            <i class="ti-truck text-primary">&#xf07a;</i>
            <span class='badge badge-warning' id='lblCartCount'>@ViewBag.quantityOrder</span>
            &nbsp; Đơn đặt hàng
        </a>
        <a class="dropdown-item" href="@Url.Action("DetailProfile","Customer",new {id=1})">
            <i class="ti-settings text-primary"></i>
            Thông tin cá nhân
        </a>
        <a class="dropdown-item" href="@Url.Action("Login","Customer")">
            <i class="ti-power-off text-primary"></i>
            Đăng xuất
        </a>
    </div>
}
else
{
    <div class="dropdown">
        <div class="d-flex d-none d-md-flex flex-row align-items-center btn">
            <a class="nav-link count-indicator dropdown-toggle" id="notificationDropdown" href="/customer/login" data-bs-toggle="dropdown">
                <span class="ti-user mx-0 mr-2"></span>Đăng nhập/Đăng kí
            </a>
        </div>
        <div class="dropdown-content" style="background-color:white">
            <a class="dropdown-item" href="@Url.Action("Register","Customer")">
                <i class="ti-user text-primary"></i>
                Đăng kí tài khoản
            </a>
            <a class="dropdown-item" href="@Url.Action("Login","Customer")">
                <i class="ti-power-off text-primary"></i>
                Đăng nhập
            </a>
        </div>
    </div>


    @*<a class="nav-link count-indicator dropdown-toggle" id="notificationDropdown" href="#" data-bs-toggle="dropdown">
        <span class="ti-user mx-0 mr-2"></span>Đăng nhập/Đăng kí
    </a>
    <div class="dropdown-menu dropdown-menu-right navbar-dropdown" aria-labelledby="profileDropdown">
        <a class="dropdown-item" href="@Url.Action("Register","Customer")">
            <i class="ti-user text-primary"></i>
            Đăng kí tài khoản
        </a>
        <a class="dropdown-item" href="@Url.Action("Login","Customer")">
            <i class="ti-power-off text-primary"></i>
            Đăng nhập
        </a>
    </div>*@
}

<style>
    .nav-link {
        position: relative;
        display: inline-block;
    }
</style>